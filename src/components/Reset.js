import React, { Component } from 'react';
import './App.css';

class Reset extends Component {
  render() {
    return (
        <button type="button" className="btn btn-primary">Resset</button>
    );
  }
}

export default Reset;
